local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Seed Buyer - Fluent " .. Fluent.Version,
    SubTitle = "by Zoro",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "leaf" }),
    Settings = Window:AddTab({ Title = "Settings", Icon = "settings" })
}

local Options = Fluent.Options

-- Seed List
local seedList = {
    "Carrot", "Strawberry", "Blueberry", "Tomato", "Cauliflower",
    "Avocado", "Banana", "Green Apple", "Bell Pepper", "Kiwi",
    "Prickly Pear", "Loquat", "Feijoa", "Sugar Apple"
}

-- Multi-select Dropdown for seeds
local SelectedSeeds = Tabs.Main:AddDropdown("SelectedSeeds", {
    Title = "🌿 Select Seeds to Auto-Buy",
    Description = "You can select multiple seeds",
    Values = seedList,
    Multi = true,
    Default = {}
})

-- Auto Buy Toggle
local AutoBuy = Tabs.Main:AddToggle("AutoBuyToggle", {
    Title = "⚡ Auto-Buy Enabled",
    Default = false
})

-- Auto-buy loop
AutoBuy:OnChanged(function(state)
    if state then
        task.spawn(function()
            while AutoBuy.Value do
                for seed, isSelected in pairs(SelectedSeeds.Value) do
                    if isSelected then
                        local args = { seed }
                        local ok, err = pcall(function()
                            game:GetService("ReplicatedStorage")
                                :WaitForChild("GameEvents")
                                :WaitForChild("BuySeedStock")
                                :FireServer(unpack(args))
                        end)
                        if not ok then
                            warn("Failed to buy seed:", seed, err)
                        end
                    end
                end
                task.wait(0.05) -- Ultra fast loop
            end
        end)
    end
end)

-- Optional: live selected seed display
Tabs.Main:AddParagraph({
    Title = "Selected Seeds",
    Content = "Shown in dropdown above."
})

-- Addon Setup
SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)

SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({})
InterfaceManager:SetFolder("FluentSeedBuyer")
SaveManager:SetFolder("FluentSeedBuyer/Configs")

InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

Window:SelectTab(1)

Fluent:Notify({
    Title = "Loaded",
    Content = "Auto Seed Buyer is ready.",
    Duration = 6
})

SaveManager:LoadAutoloadConfig()
